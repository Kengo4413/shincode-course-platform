# 05: コース一覧ページ

## 概要
トップページ (`/`) にコース一覧を表示する。各コースはカード形式で表示し、進捗率も表示する。

## Todo

### ページ (`app/page.tsx`)
- [x]Server Component として実装
- [x]`data/courses.ts` からコース一覧を取得して表示
- [x]`CourseCard` コンポーネントをグリッドレイアウトで並べる

### コンポーネント
- [x]`components/CourseCard.tsx` を作成
  - サムネイル画像（`next/image` 使用）
  - コースタイトル
  - コース説明文（truncate）
  - レッスン数
  - 進捗バー（進捗率表示）
  - コース詳細への `<Link>`
- [x]`components/ProgressBar.tsx` を作成
  - 完了レッスン数 / 全レッスン数 をパーセントで表示

### 進捗データ取得
- [x]viewer_id が localStorage にある場合、Supabase から progress を取得
- [x]コースごとの進捗率を算出するロジックを実装

### 動作確認
- [x]コース一覧がカード形式で正しく表示される
- [x]進捗バーが正しいパーセンテージを表示する
- [x]カードクリックでコース詳細ページに遷移する
- [x]レスポンシブ対応（モバイル1列 / デスクトップ複数列）
