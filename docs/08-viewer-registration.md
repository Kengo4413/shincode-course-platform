# 08: 視聴者登録モーダル

## 概要
初回アクセス時に名前/メールアドレスの入力モーダルを表示し、Supabase の `viewers` テーブルに登録、`viewer_id` を localStorage に保持する。

## Todo

### コンポーネント
- [x]`components/ViewerModal.tsx` を作成（`'use client'`）
  - 名前フィールド（必須）
  - メールアドレスフィールド（必須）
  - 送信ボタン
  - モーダルオーバーレイ（背景クリックでは閉じない）

### 登録ロジック
- [x]フォーム送信時に Supabase `viewers` テーブルへ insert
- [x]登録成功時に返却された `id` を `localStorage` に `viewer_id` として保存
- [x]同じメールアドレスが既に存在する場合は既存の `viewer_id` を取得して保存
- [x]バリデーション（メール形式チェック、空欄チェック）

### 表示制御
- [x]`localStorage` に `viewer_id` が存在するかチェック
- [x]未登録の場合のみモーダルを表示
- [x]ルートレイアウトまたは共通レイアウトにモーダルを配置

### 動作確認
- [x]初回アクセスでモーダルが表示される
- [x]名前・メール入力 → 送信で `viewers` テーブルにレコードが作成される
- [x]`localStorage` に `viewer_id` が保存される
- [x]2回目以降のアクセスではモーダルが表示されない
- [x]同じメールで再登録した場合、既存の viewer_id が使われる
