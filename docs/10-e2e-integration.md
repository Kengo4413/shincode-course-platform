# 10: E2E 結合 & 仕上げ

## 概要
全機能を結合し、ユーザーフロー全体が正しく動作することを確認する。UIの仕上げとエラーハンドリングを整備する。

## Todo

### ユーザーフロー結合テスト
- [x]初回アクセス → モーダル表示 → 登録 → コース一覧表示
- [x]コース一覧 → コース詳細 → レッスン一覧が表示される
- [x]レッスン選択 → 動画再生 →「視聴済み」→ 進捗反映
- [x]コース詳細に戻る → チェックマーク表示を確認
- [x]進捗バーのパーセンテージが正しく更新される

### エラーハンドリング
- [x]`app/not-found.tsx` を作成（グローバル404ページ）
- [x]`app/error.tsx` を作成（グローバルエラーバウンダリ）
- [x]Supabase 接続エラー時のフォールバック表示
- [x]ネットワークエラー時のリトライ UI

### UI 仕上げ
- [x]ページ間のローディング状態（`loading.tsx` または `<Suspense>`）
- [x]レスポンシブデザインの最終調整
- [x]`<meta>` タグの最終調整（OGP 等）

### ビルド確認
- [x]`npm run build` がエラーなく完了する
- [x]`npm run lint` がエラーなく完了する
- [x]本番モード (`npm start`) で全ページが正常動作する
